[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "lib-a"
version = "1.0.0"
description = "Demo library A v1 - depends on lib-b 1.x"
requires-python = ">=3.8"
dependencies = [
    "lib-b>=1.0.0,<2.0.0",
]
