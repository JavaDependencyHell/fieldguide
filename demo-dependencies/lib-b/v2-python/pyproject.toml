[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "lib-b"
version = "2.0.0"
description = "Demo library B v2 - depends on lib-c 2.x"
requires-python = ">=3.8"
dependencies = [
    "lib-c>=2.0.0",
]
