---
title: "Dependency Locking"
tagline: "Or what to Do When Today’s Build Doesn’t Match Yesterday’s"
type: scenario
tool: gradle
---

#### ! Scenario

*Or what to Do When Today’s Build Doesn’t Match Yesterday’s*
This scenario demonstrates Gradle's Dependency Locking feature.

::: {.callout-note title="Tool Versions"}
This scenario was tested with **Gradle 8.14**.
Dependency locking has been built in since **Gradle 4.8**; the single per-project `gradle.lockfile` is the default format from **Gradle 7.0**.
:::

#### ! Example

```{mermaid}
%%| fig-width: 4.4
graph LR
    Root[Project Root] --> LibA[lib-a:1.+]
```



```{.groovy include="build.gradle" snippet="s11-deps"}
```

#### ! Expect
Build today must match build tomorrow exactly.

#### ! Why
With locking activated (`dependencyLocking { lockAllConfigurations() }`), Gradle records the resolved graph in a `gradle.lockfile`. On subsequent runs, it uses these versions instead of performing dynamic resolution. Gradle is the only one of the three tools with locking built in — Maven and sbt need plugins.

#### ! Scales
Dependency locking is essential for any team that values reproducible builds, which is to say, every professional team. It is particularly critical in CI/CD pipelines, where builds must be deterministic to enable reliable testing, deployment, and rollback. Lock files should be committed to version control. Updating dependencies becomes an explicit action (./gradlew dependencies --write-locks) rather than an accidental side effect of a repository change. Many organisations combine locking with automated dependency update tools (like Dependabot or Renovate) that submit pull requests when new versions are available.

#### ! Check
**Fast check:** `./gradlew dependencies --write-locks`, then inspect `gradle.lockfile`

**What to look for:** The `gradle.lockfile` contains the exact resolved versions; subsequent `./gradlew dependencies` runs resolve to those locked versions even though the build declares `1.+`.

**Conclusion:** Locking ensures reproducibility by snapshotting dynamic versions.

#### ! Takeaway
Locking ensures reproducibility.

#### ! Related
*   [GS9: Ranges](../scenario-9-ranges/guide.qmd)
