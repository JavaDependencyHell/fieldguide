# Private Repository & Direct Patch Override


::: {.scenario}
*Or what to Do When You Need a One-Off Patch*
This scenario shows a more direct way to use a private, third-party repository. Instead of using a vendor-supplied BOM, we explicitly request the patched version of a dependency in our project's `dependencies` block.
This approach is simpler than the BOM method but can be more verbose if many dependencies need to be overridden. It is suitable for one-off patches or when a full BOM is not available.
:::

```{mermaid}
%%| fig-width: 4.4
graph LR
    subgraph Public Repos
        SpringBoot["org.springframework.boot:spring-boot-starter:2.5.14 (EOL)"]
    end
    subgraph Private Repo
        PatchedStarter["org.springframework.boot:spring-boot-starter:2.5.14.ACME"]
    end
    Root[Project Root] -- declares --> PatchedStarter

```

#### Gradle (build.gradle)
The `build.gradle` adds the private repository and then directly declares a dependency on the specific patched version.

```{.groovy include="/demos/gradle-demo/scenario-17-private-patch/build.gradle" snippet="repo-and-deps"}
```

#### Reader Expectation
Directly declaring a patched version from a private repository ensures that exact artifact is used over any public release.

#### Why This Happens
A direct declaration in the `dependencies` block acts as a powerful resolution signal. When combined with a private repository that makes the specific patched version available, Gradle selects it to fulfill the project's requirement.

#### Key Takeaway
A direct version declaration for a dependency is the most straightforward way to ensure a specific version is used, provided that version is available in one of the configured repositories.

#### Related Scenarios
*   [Scenario 16: Private Repository & BOM Override](../scenario-16-private-bom/guide.qmd) - A more scalable approach using a vendor BOM.
