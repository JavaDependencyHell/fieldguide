[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "scenario-2-conflict"
version = "0.1.0"
description = "Diamond dependency conflict demo"
requires-python = ">=3.8"
dependencies = [
    "lib-a==2.0.0",
    "lib-b==1.0.0",
]
